import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'br.com.triboni.plana',
  appName: 'frontend-challenge-daniel-triboni',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
